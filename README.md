# SmartIntake AI Agent

Initial metadata sync from the Salesforce org:

- Org alias: `SmartIntake New Org`
- Username: `sivachandran.m.10e696da59e6@agentforce.com`
- Base sync branch: `base/org-initial-sync-2026-07-20`

This repository uses the Salesforce org as the source of truth for SmartIntake metadata. Embedded Service and Messaging Channel metadata are intentionally excluded from this baseline because those channels are created and published manually per org.

Future code or metadata changes should be made on feature or bugfix branches from this base branch.
